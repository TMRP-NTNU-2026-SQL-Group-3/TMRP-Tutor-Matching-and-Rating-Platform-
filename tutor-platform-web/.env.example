# Vite picks up .env.development in dev and .env.production in prod builds.
# Copy this file to .env.local to override either for your local workflow.
# Leave empty for Docker prod (nginx proxies /api/* to the api service).
VITE_API_BASE_URL=http://localhost:8000
