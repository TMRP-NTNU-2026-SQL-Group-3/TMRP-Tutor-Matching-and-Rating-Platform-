# Dev-only overrides. docker compose auto-loads this file when you run
# `docker compose up` without `-f`. In production invoke compose explicitly
# so this file is NOT applied:
#   docker compose -f docker-compose.yml up -d
#
# HIGH-5: host port publishing for the database lives here so production
# never exposes the database on the host. Inter-container traffic (api -> db)
# uses the compose network and does not need this binding.
#
# Ports use the project's uncommon 41xxx scheme (api 41000, db 41432).
services:
  db:
    ports:
      - "127.0.0.1:41432:41432"
  api:
    ports:
      - "127.0.0.1:41000:41000"
