# docker-compose 變數插值來源（位於 repo 根目錄）
# 複製為 `.env` 後修改值；本檔由 .gitignore 排除。
#   cp .env.example .env

# PostgreSQL 帳密 — 同時供 db 容器 (POSTGRES_*) 與 api/worker 容器
# (DATABASE_URL) 使用。請在第一次部署時改為強隨機密碼。
DB_USER=tmrp
DB_PASSWORD=please_change_me_to_a_strong_random_password
DB_NAME=tmrp
