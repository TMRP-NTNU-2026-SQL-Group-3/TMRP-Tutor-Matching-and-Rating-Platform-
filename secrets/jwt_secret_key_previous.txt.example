# Leave empty unless you are mid-rotation. If populated, must be >= 32 chars
# and differ from jwt_secret_key.txt. Clear (truncate to empty) once the
# access-token TTL window from the last rotation has elapsed.
